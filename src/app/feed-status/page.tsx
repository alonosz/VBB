import { FeedStatusChecker } from "@/components/feed/FeedStatusChecker";

export const metadata = {
  title: "Is Google collecting your values? · VBB",
};

export default function FeedStatusPage() {
  return <FeedStatusChecker />;
}
